# view 身份标记贯穿全链路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把视图身份(kind)从三个 introspector 一路带到 `TableInfo`、生成 YAML、元数据库 `meta_table`、管理 API,视图与表真正可区分。

**Architecture:** `kind` 是贯穿元数据链路的规范字符串(`table`/`view`/`materialized_view`),各引擎在 introspector 内私有映射,`TableKind` 只承载常量与校验。YAML 里 `kind` 是可选属性、仅非 `table` 时输出;改写端 `YamlConfigLoader` 手工取键,未知键天然忽略,零改动。

**Tech Stack:** Java 17(record/switch 表达式)、JUnit 5 + Mockito(内省测试,mock `Connection`/`ResultSet`)、zonkey EmbeddedPostgres(store 测试)、SnakeYAML、Spring JDBC。

**Spec:** `docs/superpowers/specs/2026-09-17-view-kind-design.md`(计划从 spec 论证,执行者两份都读)

## Global Constraints

- kind 合法值恰好三个,小写:`table`、`view`、`materialized_view`(spec §1)。
- `TableInfo`/`TableStructure` 必须保留旧四参构造器,缺省 `kind = "table"`;现有调用点与测试不允许大面积改动(spec §2/§4)。
- `MetadataYamlGenerator` 仅在 `kind != "table"` 时输出 `kind:` 行;无视图库的输出与改动前**逐字节一致**,现有 golden 文件零改动(spec §3)。
- importer 对非法 kind 报 `CONFIG_ERROR` 且消息含合法值列表,不静默丢弃(spec §3/§5)。
- introspector 遇到未知源值:不失败,降级 `table` 并追加一条 warning(spec §1/§5)。
- 存量库迁移用 `ALTER TABLE meta_table ADD COLUMN IF NOT EXISTS kind VARCHAR(32) NOT NULL DEFAULT 'table';` 追加在 `metadata-schema.sql` 末尾(`spring.sql.init.mode: always` 每次启动幂等执行)(spec §4)。
- mask-core 改写端(`YamlConfigLoader`/`TableMetadata`)与 mask-lite 生产代码零改动;只补容忍契约测试(spec 非目标)。
- 提交信息沿用仓库惯例:`feat(module): 中文描述`。
- 环境:Windows + Git Bash;构建命令 `mvn -pl <module> -am test -Dtest=<Class> -DfailIfNoTests=false`(单测)/ `mvn test`(全量)。

---

### Task 1: `TableKind` 常量类与 `TableInfo.kind` 组件

**Files:**
- Create: `mask-core/src/main/java/io/sqlmask/introspect/TableKind.java`
- Modify: `mask-core/src/main/java/io/sqlmask/introspect/IntrospectionResult.java:23`
- Test: `mask-core/src/test/java/io/sqlmask/introspect/IntrospectionResultTest.java`(新建)

**Interfaces:**
- Consumes: 无(链路源头)。
- Produces: `TableKind.TABLE`/`TableKind.VIEW`/`TableKind.MATERIALIZED_VIEW`(常量 `"table"`/`"view"`/`"materialized_view"`)、`TableKind.isKnown(String)`;`IntrospectionResult.TableInfo` 变为 `(String catalog, String schema, String name, String kind, List<ColumnInfo> columns)`,`kind()` 访问器,四参构造器 `TableInfo(catalog, schema, name, columns)` 缺省 `"table"`。后续所有任务依赖这些签名。

- [ ] **Step 1: 写失败测试**

新建 `mask-core/src/test/java/io/sqlmask/introspect/IntrospectionResultTest.java`:

```java
package io.sqlmask.introspect;

import org.junit.jupiter.api.Test;

import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;

class IntrospectionResultTest {

  @Test
  void fourArgConstructorDefaultsKindToTable() {
    IntrospectionResult.TableInfo table = new IntrospectionResult.TableInfo(
        "crm", "public", "customer", List.of());
    assertEquals("table", table.kind());
  }

  @Test
  void explicitKindIsCarried() {
    IntrospectionResult.TableInfo table = new IntrospectionResult.TableInfo(
        "crm", "public", "customer_v", "view", List.of());
    assertEquals("view", table.kind());
  }

  @Test
  void blankOrNullKindNormalizesToTable() {
    assertEquals("table", new IntrospectionResult.TableInfo(
        "crm", "public", "t", (String) null, List.of()).kind());
    assertEquals("table", new IntrospectionResult.TableInfo(
        "crm", "public", "t", "  ", List.of()).kind());
  }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-core -am test -Dtest=IntrospectionResultTest -DfailIfNoTests=false`
Expected: 编译失败,`TableInfo` 无五参构造 / 无 `kind()` 访问器。

- [ ] **Step 3: 最小实现**

新建 `mask-core/src/main/java/io/sqlmask/introspect/TableKind.java`:

```java
package io.sqlmask.introspect;

/**
 * The normalized table kinds carried through the metadata chain. Source
 * values differ per engine (PG relkind, MySQL/Trino TABLE_TYPE); each
 * introspector maps its own values onto these constants.
 */
public final class TableKind {

  public static final String TABLE = "table";
  public static final String VIEW = "view";
  public static final String MATERIALIZED_VIEW = "materialized_view";

  private TableKind() {
  }

  /** True when {@code kind} is one of the three legal values. */
  public static boolean isKnown(String kind) {
    return TABLE.equals(kind) || VIEW.equals(kind) || MATERIALIZED_VIEW.equals(kind);
  }
}
```

把 `IntrospectionResult.java` 中的 `TableInfo` record(原第 23-24 行)替换为:

```java
  /**
   * One captured table. {@code kind} is the normalized {@link TableKind}
   * value. {@code columns} intentionally keeps the list the introspector
   * appends to while assembling — no defensive copy here, because
   * {@code List.copyOf} would reject the adds; the snapshot in the enclosing
   * {@link IntrospectionResult} compact constructor is what freezes the result.
   */
  public record TableInfo(String catalog, String schema, String name, String kind,
      List<ColumnInfo> columns) {

    public TableInfo {
      kind = kind == null || kind.isBlank() ? TableKind.TABLE : kind;
    }

    /** Convenience constructor for plain tables (kind defaults to "table"). */
    public TableInfo(String catalog, String schema, String name, List<ColumnInfo> columns) {
      this(catalog, schema, name, TableKind.TABLE, columns);
    }
  }
```

注意:紧凑构造器**不得**对 `columns` 做 `List.copyOf`(保留现有"组装期可追加"语义,见上方 javadoc)。

- [ ] **Step 4: 跑测试确认通过 + 内省包回归**

Run: `mvn -pl mask-core -am test -Dtest='IntrospectionResultTest,PgMetadataIntrospectorTest,MysqlMetadataIntrospectorTest,TrinoMetadataIntrospectorTest,MetadataYamlGeneratorTest' -DfailIfNoTests=false`
Expected: 全部 PASS(四参构造器保证既有测试编译运行不变)。

- [ ] **Step 5: 提交**

```bash
git add mask-core/src/main/java/io/sqlmask/introspect/TableKind.java \
  mask-core/src/main/java/io/sqlmask/introspect/IntrospectionResult.java \
  mask-core/src/test/java/io/sqlmask/introspect/IntrospectionResultTest.java
git commit -m "feat(introspect): TableKind 常量与 TableInfo.kind 组件（缺省 table）"
```

---

### Task 2: PG introspector `relkind` → kind

**Files:**
- Modify: `mask-core/src/main/java/io/sqlmask/introspect/PgMetadataIntrospector.java`(introspect/queryTables/assemble 三个方法)
- Test: `mask-core/src/test/java/io/sqlmask/introspect/PgMetadataIntrospectorTest.java`

**Interfaces:**
- Consumes: Task 1 的 `TableKind` 常量与五参 `TableInfo`。
- Produces: PG 采集出的每个 `TableInfo.kind()` 已填充(`'r'/'p'`→`table`、`'v'`→`view`、`'m'`→`materialized_view`);未知 relkind 降级 `table` 并向 warnings 追加 `unknown relkind 'x' for <catalog>.<schema>.<name>, degraded to table`。

- [ ] **Step 1: 写失败测试**

在 `PgMetadataIntrospectorTest` 中,先给既有 `collectsTablesColumnsAndCatalog` 的断言区追加一行(在 `assertEquals(2, customer.columns().size());` 之后):

```java
    assertEquals("table", customer.kind());
```

再新增测试方法:

```java
  @Test
  void viewKindsAreMappedAndUnknownRelkindDegradesWithWarning() throws SQLException {
    // 每行: schema, table, relkind, column, pg_type, attnum
    ResultSet tables = mock(ResultSet.class);
    when(tables.next()).thenReturn(true, true, true, false);
    when(tables.getString("schema_name")).thenReturn("public", "public", "sales");
    when(tables.getString("table_name")).thenReturn("customer_v", "mv_stats", "weird");
    when(tables.getString("relkind")).thenReturn("v", "m", "x");
    when(tables.getString("column_name")).thenReturn("id", "id", "id");
    when(tables.getString("pg_type")).thenReturn("bigint", "bigint", "bigint");
    when(tables.getInt("attnum")).thenReturn(1, 1, 1);

    Connection conn = fakeConnection(singleStringRow("crm"), tables);
    PgMetadataIntrospector introspector = new PgMetadataIntrospector() {
      @Override protected Connection open(ConnectionSpec spec) { return conn; }
    };

    IntrospectionResult result = introspector.introspect(
        new ConnectionSpec("postgresql", "h", 5432, "crm", "u", "p", List.of(), true, false, "disable", 10));

    assertEquals("view", result.tables().get(0).kind());
    assertEquals("materialized_view", result.tables().get(1).kind());
    assertEquals("table", result.tables().get(2).kind());
    assertTrue(result.warnings().stream()
        .anyMatch(w -> w.contains("unknown relkind 'x'") && w.contains("crm.sales.weird")));
  }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-core -am test -Dtest=PgMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: `viewKindsAreMappedAndUnknownRelkindDegradesWithWarning` FAIL(view 表 kind 是默认 `table`)。

- [ ] **Step 3: 最小实现**

`PgMetadataIntrospector.java` 改四处:

(a) `introspect()` 把 warnings 的创建提前并传给 `queryTables`(原第 44-45 行两行):

```java
      String catalog = queryCurrentDatabase(connection);
      List<String> warnings = new ArrayList<>();
      List<IntrospectionResult.TableInfo> tables = queryTables(connection, spec, catalog, warnings);
```

(b) `queryTables` 加参数并在调用 `assemble` 时透传(方法签名与最后一行):

```java
  private List<IntrospectionResult.TableInfo> queryTables(
      Connection connection, ConnectionSpec spec, String catalog,
      List<String> warnings) throws SQLException {
```

```java
      try (ResultSet rs = statement.executeQuery()) {
        return assemble(rs, catalog, warnings);
      }
```

(c) `assemble` 整体替换为(需补 `import java.util.HashSet;` 与 `import java.util.Set;`):

```java
  private List<IntrospectionResult.TableInfo> assemble(
      ResultSet rs, String catalog, List<String> warnings) throws SQLException {
    Map<String, IntrospectionResult.TableInfo> byKey = new LinkedHashMap<>();
    Set<String> unknownKindTables = new HashSet<>();
    while (rs.next()) {
      String schema = rs.getString("schema_name");
      String name = rs.getString("table_name");
      String column = rs.getString("column_name");
      String pgType = rs.getString("pg_type");
      PgTypeMapper.Mapped mapped = typeMapper.map(pgType);
      String key = schema + "." + name;
      IntrospectionResult.TableInfo table = byKey.get(key);
      if (table == null) {
        String relKind = rs.getString("relkind");
        String kind = kindOf(relKind);
        if (kind == null) {
          kind = TableKind.TABLE;
          if (unknownKindTables.add(key)) {
            warnings.add("unknown relkind '" + relKind + "' for "
                + catalog + "." + schema + "." + name + ", degraded to table");
          }
        }
        table = new IntrospectionResult.TableInfo(catalog, schema, name, kind, new ArrayList<>());
        byKey.put(key, table);
      }
      table.columns().add(new IntrospectionResult.ColumnInfo(
          column, mapped.yamlType(), pgType, mapped.degraded()));
    }
    return new ArrayList<>(byKey.values());
  }

  /** Maps a pg_class.relkind value; null means unrecognized (caller degrades). */
  private static String kindOf(String relKind) {
    return switch (relKind) {
      case "v" -> TableKind.VIEW;
      case "m" -> TableKind.MATERIALIZED_VIEW;
      case "r", "p" -> TableKind.TABLE;
      default -> null;
    };
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mvn -pl mask-core -am test -Dtest=PgMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: PASS(含既有三个测试)。

- [ ] **Step 5: 提交**

```bash
git add mask-core/src/main/java/io/sqlmask/introspect/PgMetadataIntrospector.java \
  mask-core/src/test/java/io/sqlmask/introspect/PgMetadataIntrospectorTest.java
git commit -m "feat(introspect): PG relkind → kind 映射（未知值降级 table + warning）"
```

---

### Task 3: MySQL introspector `TABLE_TYPE` → kind

**Files:**
- Modify: `mask-core/src/main/java/io/sqlmask/introspect/MysqlMetadataIntrospector.java`(introspect/queryTables/assemble)
- Test: `mask-core/src/test/java/io/sqlmask/introspect/MysqlMetadataIntrospectorTest.java`

**Interfaces:**
- Consumes: Task 1 的 `TableKind`。
- Produces: MySQL 采集的 `TableInfo.kind()` 已填充(`BASE TABLE`→`table`;`VIEW`/`SYSTEM VIEW`→`view`);未知值降级 `table` + warning `unknown TABLE_TYPE 'x' for <catalog>.<schema>.<name>, degraded to table`。

- [ ] **Step 1: 写失败测试**

在 `MysqlMetadataIntrospectorTest` 的 `collectsTablesColumnsAndCatalog` 断言区追加:

```java
    assertEquals("table", result.tables().get(0).kind());
```

新增测试方法(夹具风格与该文件既有测试一致,内联建 mock):

```java
  @Test
  void viewKindsAreMappedAndUnknownTableTypeDegradesWithWarning() throws SQLException {
    Connection conn = mock(Connection.class);
    PreparedStatement c1 = mock(PreparedStatement.class);
    PreparedStatement c2 = mock(PreparedStatement.class);
    when(conn.prepareStatement(anyString())).thenReturn(c1, c2);

    ResultSet db = mock(ResultSet.class);
    when(db.next()).thenReturn(true, false);
    when(db.getString(1)).thenReturn("shop");

    ResultSet rows = mock(ResultSet.class);
    // 每行: TABLE_SCHEMA, TABLE_NAME, TABLE_TYPE, COLUMN_NAME, COLUMN_TYPE
    when(rows.next()).thenReturn(true, true, true, false);
    when(rows.getString("TABLE_SCHEMA")).thenReturn("shop", "shop", "shop");
    when(rows.getString("TABLE_NAME")).thenReturn("customer_v", "system_v", "weird");
    when(rows.getString("TABLE_TYPE")).thenReturn("VIEW", "SYSTEM VIEW", "WHATEVER");
    when(rows.getString("COLUMN_NAME")).thenReturn("id", "id", "id");
    when(rows.getString("COLUMN_TYPE")).thenReturn("bigint", "bigint", "bigint");

    when(c1.executeQuery()).thenReturn(db);
    when(c2.executeQuery()).thenReturn(rows);

    MysqlMetadataIntrospector introspector = new MysqlMetadataIntrospector() {
      @Override protected Connection open(ConnectionSpec spec) { return conn; }
    };
    IntrospectionResult result = introspector.introspect(
        new ConnectionSpec("mysql", "h", 3306, "shop", "u", "p", List.of(), true, false, "disable", 10));

    assertEquals("view", result.tables().get(0).kind());
    assertEquals("view", result.tables().get(1).kind());
    assertEquals("table", result.tables().get(2).kind());
    assertTrue(result.warnings().stream()
        .anyMatch(w -> w.contains("unknown TABLE_TYPE 'WHATEVER'") && w.contains("shop.shop.weird")));
  }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-core -am test -Dtest=MysqlMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: 新测试 FAIL。

- [ ] **Step 3: 最小实现**

`MysqlMetadataIntrospector.java` 改四处:

(a) `introspect()` 中(原第 44-46 行)warnings 提前:

```java
      String catalog = queryCurrentDatabase(connection);
      List<String> warnings = new ArrayList<>();
      List<IntrospectionResult.TableInfo> tables = queryTables(connection, spec, catalog, warnings);
```

(b) `queryTables` 签名与尾部调用:

```java
  private List<IntrospectionResult.TableInfo> queryTables(
      Connection connection, ConnectionSpec spec, String catalog,
      List<String> warnings) throws SQLException {
```

```java
      try (ResultSet rs = statement.executeQuery()) {
        return assemble(rs, catalog, warnings);
      }
```

(c) `assemble` 整体替换(需补 `import java.util.HashSet;`、`import java.util.Set;`):

```java
  private List<IntrospectionResult.TableInfo> assemble(
      ResultSet rs, String catalog, List<String> warnings) throws SQLException {
    Map<String, IntrospectionResult.TableInfo> byKey = new LinkedHashMap<>();
    Set<String> unknownKindTables = new HashSet<>();
    while (rs.next()) {
      String schema = rs.getString("TABLE_SCHEMA");
      String name = rs.getString("TABLE_NAME");
      String column = rs.getString("COLUMN_NAME");
      String columnType = rs.getString("COLUMN_TYPE");
      PgTypeMapper.Mapped mapped = typeMapper.map(columnType);
      String key = schema + "." + name;
      IntrospectionResult.TableInfo table = byKey.get(key);
      if (table == null) {
        String tableType = rs.getString("TABLE_TYPE");
        String kind = kindOf(tableType);
        if (kind == null) {
          kind = TableKind.TABLE;
          if (unknownKindTables.add(key)) {
            warnings.add("unknown TABLE_TYPE '" + tableType + "' for "
                + catalog + "." + schema + "." + name + ", degraded to table");
          }
        }
        table = new IntrospectionResult.TableInfo(catalog, schema, name, kind, new ArrayList<>());
        byKey.put(key, table);
      }
      table.columns().add(new IntrospectionResult.ColumnInfo(column, mapped.yamlType(),
          columnType, mapped.degraded()));
    }
    return new ArrayList<>(byKey.values());
  }

  /** Maps an information_schema TABLE_TYPE value; null means unrecognized. */
  private static String kindOf(String tableType) {
    return switch (tableType) {
      case "VIEW", "SYSTEM VIEW" -> TableKind.VIEW;
      case "BASE TABLE" -> TableKind.TABLE;
      default -> null;
    };
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mvn -pl mask-core -am test -Dtest=MysqlMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add mask-core/src/main/java/io/sqlmask/introspect/MysqlMetadataIntrospector.java \
  mask-core/src/test/java/io/sqlmask/introspect/MysqlMetadataIntrospectorTest.java
git commit -m "feat(introspect): MySQL TABLE_TYPE → kind 映射（含 SYSTEM VIEW）"
```

---

### Task 4: Trino introspector 选列 `TABLE_TYPE` 并映射 kind

**Files:**
- Modify: `mask-core/src/main/java/io/sqlmask/introspect/TrinoMetadataIntrospector.java`(TABLES_SQL 第 30 行、introspect/queryTables/assemble)
- Test: `mask-core/src/test/java/io/sqlmask/introspect/TrinoMetadataIntrospectorTest.java`

**Interfaces:**
- Consumes: Task 1 的 `TableKind`。
- Produces: Trino 采集的 `TableInfo.kind()` 已填充(`BASE TABLE`→`table`、`VIEW`→`view`);未知值降级 `table` + warning(文案同 MySQL 的 `unknown TABLE_TYPE ...`)。注意:Trino 原 SQL **未选** `t.TABLE_TYPE`,本任务补上。

- [ ] **Step 1: 写失败测试**

在 `TrinoMetadataIntrospectorTest` 的 `collectsTablesColumnsAndCatalog` 中做两处修改:

(a) 给 `rows` mock 补 stub(既有夹具没 stub `TABLE_TYPE`,实现读到 null 会在 switch 上 NPE;三行同表,单值即可):

```java
    when(rows.getString("TABLE_TYPE")).thenReturn("BASE TABLE");
```

(b) 断言区追加:

```java
    assertEquals("table", result.tables().get(0).kind());
```

新增测试方法(注意:Trino 只有一条查询,单 PreparedStatement):

```java
  @Test
  void viewKindsAreMappedAndUnknownTableTypeDegradesWithWarning() throws SQLException {
    Connection conn = mock(Connection.class);
    PreparedStatement c1 = mock(PreparedStatement.class);
    when(conn.prepareStatement(anyString())).thenReturn(c1);

    ResultSet rows = mock(ResultSet.class);
    // 每行: TABLE_SCHEMA, TABLE_NAME, TABLE_TYPE, COLUMN_NAME, DATA_TYPE
    when(rows.next()).thenReturn(true, true, true, false);
    when(rows.getString("TABLE_SCHEMA")).thenReturn("shop", "shop", "shop");
    when(rows.getString("TABLE_NAME")).thenReturn("customer_v", "orders_v", "weird");
    when(rows.getString("TABLE_TYPE")).thenReturn("VIEW", "VIEW", "WHATEVER");
    when(rows.getString("COLUMN_NAME")).thenReturn("id", "id", "id");
    when(rows.getString("DATA_TYPE")).thenReturn("bigint", "bigint", "bigint");
    when(c1.executeQuery()).thenReturn(rows);

    TrinoMetadataIntrospector introspector = new TrinoMetadataIntrospector() {
      @Override protected Connection open(ConnectionSpec spec) { return conn; }
    };
    IntrospectionResult result = introspector.introspect(
        new ConnectionSpec("trino", "h", 8080, "shop", "u", "p", List.of(), true, false, "disable", 10));

    assertEquals("view", result.tables().get(0).kind());
    assertEquals("view", result.tables().get(1).kind());
    assertEquals("table", result.tables().get(2).kind());
    assertTrue(result.warnings().stream()
        .anyMatch(w -> w.contains("unknown TABLE_TYPE 'WHATEVER'") && w.contains("shop.shop.weird")));
  }
```

同时在 `collectsTablesColumnsAndCatalog` 的 SQL 断言区(既有 `assertTrue(sql.getValue().contains(...))` 附近)补一条,锁定 SELECT 列表带出了新列(WHERE 里的 `t.TABLE_TYPE IN` 本来就存在,断言必须覆盖 SELECT 列表才算数):

```java
    assertTrue(sql.getValue().contains("c.TABLE_NAME, t.TABLE_TYPE, c.COLUMN_NAME"));
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-core -am test -Dtest=TrinoMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: 新测试 FAIL。

- [ ] **Step 3: 最小实现**

`TrinoMetadataIntrospector.java` 改四处:

(a) `TABLES_SQL` 的 SELECT 列表加 `t.TABLE_TYPE`(放 `c.TABLE_NAME` 之后):

```java
  private static final String TABLES_SQL = """
      SELECT c.TABLE_SCHEMA, c.TABLE_NAME, t.TABLE_TYPE, c.COLUMN_NAME, c.DATA_TYPE, c.ORDINAL_POSITION
      FROM information_schema.columns c
      JOIN information_schema.tables t
        ON t.TABLE_SCHEMA = c.TABLE_SCHEMA AND t.TABLE_NAME = c.TABLE_NAME
      WHERE %s
        AND t.TABLE_TYPE IN (%s)
      ORDER BY c.TABLE_SCHEMA, c.TABLE_NAME, c.ORDINAL_POSITION""";
```

(b) `introspect()` 中(原第 42-44 行)warnings 提前:

```java
      List<String> warnings = new ArrayList<>();
      List<IntrospectionResult.TableInfo> tables =
          queryTables(connection, spec, spec.database(), warnings);
```

(c) `queryTables` 签名与尾部调用(同 Task 2/3 模式):

```java
  private List<IntrospectionResult.TableInfo> queryTables(
      Connection connection, ConnectionSpec spec, String catalog,
      List<String> warnings) throws SQLException {
```

```java
      try (ResultSet rs = statement.executeQuery()) {
        return assemble(rs, catalog, warnings);
      }
```

(d) `assemble` 整体替换(需补 `import java.util.HashSet;`、`import java.util.Set;`):

```java
  private List<IntrospectionResult.TableInfo> assemble(
      ResultSet rs, String catalog, List<String> warnings) throws SQLException {
    Map<String, IntrospectionResult.TableInfo> byKey = new LinkedHashMap<>();
    Set<String> unknownKindTables = new HashSet<>();
    while (rs.next()) {
      String schema = rs.getString("TABLE_SCHEMA");
      String name = rs.getString("TABLE_NAME");
      String column = rs.getString("COLUMN_NAME");
      String dataType = rs.getString("DATA_TYPE");
      PgTypeMapper.Mapped mapped = typeMapper.map(dataType);
      String key = schema + "." + name;
      IntrospectionResult.TableInfo table = byKey.get(key);
      if (table == null) {
        String tableType = rs.getString("TABLE_TYPE");
        String kind = kindOf(tableType);
        if (kind == null) {
          kind = TableKind.TABLE;
          if (unknownKindTables.add(key)) {
            warnings.add("unknown TABLE_TYPE '" + tableType + "' for "
                + catalog + "." + schema + "." + name + ", degraded to table");
          }
        }
        table = new IntrospectionResult.TableInfo(catalog, schema, name, kind, new ArrayList<>());
        byKey.put(key, table);
      }
      table.columns().add(new IntrospectionResult.ColumnInfo(column, mapped.yamlType(),
          dataType, mapped.degraded()));
    }
    return new ArrayList<>(byKey.values());
  }

  /** Maps an information_schema TABLE_TYPE value; null means unrecognized. */
  private static String kindOf(String tableType) {
    return switch (tableType) {
      case "VIEW" -> TableKind.VIEW;
      case "BASE TABLE" -> TableKind.TABLE;
      default -> null;
    };
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mvn -pl mask-core -am test -Dtest=TrinoMetadataIntrospectorTest -DfailIfNoTests=false`
Expected: PASS(含 ArgumentCaptor 的 SQL 断言)。

- [ ] **Step 5: 提交**

```bash
git add mask-core/src/main/java/io/sqlmask/introspect/TrinoMetadataIntrospector.java \
  mask-core/src/test/java/io/sqlmask/introspect/TrinoMetadataIntrospectorTest.java
git commit -m "feat(introspect): Trino TABLE_TYPE 选列与 kind 映射"
```

---

### Task 5: `MetadataYamlGenerator` 条件输出 kind 行 + views golden

**Files:**
- Modify: `mask-core/src/main/java/io/sqlmask/introspect/MetadataYamlGenerator.java:26-35`
- Test: `mask-core/src/test/java/io/sqlmask/introspect/MetadataYamlGeneratorTest.java`
- Test data(由 `-Dgolden.write=true` 生成,人工确认后入库): `mask-core/src/test/resources/golden/introspect-views.yaml`

**Interfaces:**
- Consumes: Task 1 的 `TableKind.TABLE` 与 `TableInfo.kind()`。
- Produces: YAML 契约——`kind:` 行位于 `name:` 之后、`columns:` 之前,缩进 6 空格,仅非 `table` 输出。

- [ ] **Step 1: 写失败测试**

`MetadataYamlGeneratorTest` 新增夹具与测试(import 区补 `static org.junit.jupiter.api.Assertions.assertFalse`):

```java
  private IntrospectionResult views() {
    return new IntrospectionResult("crm", List.of(
        new IntrospectionResult.TableInfo("crm", "public", "customer", List.of(
            new IntrospectionResult.ColumnInfo("id", "bigint", "bigint", false))),
        new IntrospectionResult.TableInfo("crm", "public", "customer_v", "view", List.of(
            new IntrospectionResult.ColumnInfo("id", "bigint", "bigint", false))),
        new IntrospectionResult.TableInfo("crm", "public", "mv_stats", "materialized_view", List.of(
            new IntrospectionResult.ColumnInfo("day", "date", "date", false)))),
        List.of());
  }

  @Test
  void viewsGolden() throws Exception {
    assertGolden("introspect-views.yaml", views());
  }

  @Test
  void kindOmittedForPlainTables() {
    String out = new MetadataYamlGenerator().generate(multiTable());
    assertFalse(out.contains("kind:"));
  }
```

预期 golden 内容(`mvn ... -Dgolden.write=true` 生成后与下面逐字对比;排序 catalog→schema→name,`customer` < `customer_v` < `mv_stats`):

```yaml
metadata:
  tables:
    - catalog: crm
      schema: public
      name: customer
      columns:
        - name: id
          type: bigint
    - catalog: crm
      schema: public
      name: customer_v
      kind: view
      columns:
        - name: id
          type: bigint
    - catalog: crm
      schema: public
      name: mv_stats
      kind: materialized_view
      columns:
        - name: day
          type: date
policies: {}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-core -am test -Dtest=MetadataYamlGeneratorTest -DfailIfNoTests=false`
Expected: `viewsGolden` FAIL(golden 文件不存在);`kindOmittedForPlainTables` 应已 PASS(现实现不输出 kind)。

- [ ] **Step 3: 最小实现**

`MetadataYamlGenerator.generate` 的表循环(原第 26-30 行)在 `name:` 行后插入条件输出:

```java
      for (IntrospectionResult.TableInfo table : tables) {
        out.append("    - catalog: ").append(scalar(table.catalog())).append('\n');
        out.append("      schema: ").append(scalar(table.schema())).append('\n');
        out.append("      name: ").append(scalar(table.name())).append('\n');
        if (!TableKind.TABLE.equals(table.kind())) {
          out.append("      kind: ").append(table.kind()).append('\n');
        }
        out.append("      columns:\n");
```

- [ ] **Step 4: 生成新 golden 并全量回归该测试类**

Run: `mvn -pl mask-core -am test -Dtest=MetadataYamlGeneratorTest -DfailIfNoTests=false -Dgolden.write=true`
然后检查 `git status`:**只允许**新增 `mask-core/src/test/resources/golden/introspect-views.yaml`,既有五个 golden 文件必须零改动(`git diff --stat mask-core/src/test/resources/golden/` 应只显示新文件);内容与 Step 1 逐字一致后,再跑一次不带 `-Dgolden.write` 的命令确认 PASS。

- [ ] **Step 5: 提交**

```bash
git add mask-core/src/main/java/io/sqlmask/introspect/MetadataYamlGenerator.java \
  mask-core/src/test/java/io/sqlmask/introspect/MetadataYamlGeneratorTest.java \
  mask-core/src/test/resources/golden/introspect-views.yaml
git commit -m "feat(introspect): YAML 生成器条件输出 kind 行（无视图库字节不变）"
```

---

### Task 6: `TableStructure.kind` 与 importer 可选解析 + 枚举校验

**Files:**
- Modify: `mask-metadata/src/main/java/io/sqlmask/metaserver/model/TableStructure.java`
- Modify: `mask-metadata/src/main/java/io/sqlmask/metaserver/service/MetadataYamlImporter.java`(parseTable)
- Test: `mask-metadata/src/test/java/io/sqlmask/metaserver/service/MetadataYamlImporterTest.java`

**Interfaces:**
- Consumes: mask-core 的 `io.sqlmask.introspect.TableKind`(mask-metadata 已依赖 mask-core,`CollectService` 已 import 同包类)。
- Produces: `TableStructure` 变为 `(String catalog, String schema, String name, String kind, List<ColumnStructure> columns)`,`kind()` 访问器,四参构造器缺省 `"table"`;importer 接受可选 `kind`(缺省 `"table"`,非法值 `CONFIG_ERROR`)。Task 7/8 依赖。

- [ ] **Step 1: 写失败测试**

`MetadataYamlImporterTest` 新增(import 区补 `io.sqlmask.introspect.TableKind` 不需要;断言用字符串字面量):

```java
  @Test
  void kindDefaultsToTableWhenAbsent() {
    String yaml = """
        metadata:
          tables:
            - catalog: crm
              schema: public
              name: customer
              columns:
                - { name: id, type: bigint }
        """;
    assertEquals("table", importer.parse(yaml, "test.yaml").get(0).kind());
  }

  @Test
  void viewAndMaterializedViewKindsAccepted() {
    String yaml = """
        metadata:
          tables:
            - catalog: crm
              schema: public
              name: customer_v
              kind: view
              columns:
                - { name: id, type: bigint }
            - catalog: crm
              schema: public
              name: mv_stats
              kind: materialized_view
              columns:
                - { name: day, type: date }
        """;
    List<TableStructure> tables = importer.parse(yaml, "test.yaml");
    assertEquals("view", tables.get(0).kind());
    assertEquals("materialized_view", tables.get(1).kind());
  }

  @Test
  void unknownKindRejectedNotSilentlyDropped() {
    String yaml = """
        metadata:
          tables:
            - catalog: crm
              schema: public
              name: customer_v
              kind: viwe
              columns:
                - { name: id, type: bigint }
        """;
    SqlMaskException e = assertThrows(SqlMaskException.class, () -> importer.parse(yaml, "test.yaml"));
    assertEquals(SqlMaskException.Code.CONFIG_ERROR, e.getCode());
    assertTrue(e.getMessage().contains("unknown kind 'viwe'"), () -> e.getMessage());
    assertTrue(e.getMessage().contains("table|view|materialized_view"), () -> e.getMessage());
  }
```

同时在既有 `parsesMetadataTables` 断言区追加:

```java
    assertEquals("table", tables.get(0).kind());
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-metadata -am test -Dtest=MetadataYamlImporterTest -DfailIfNoTests=false`
Expected: 编译失败(`TableStructure` 无 `kind()`),或新断言 FAIL。

- [ ] **Step 3: 最小实现**

`TableStructure.java` 全文件替换:

```java
package io.sqlmask.metaserver.model;

import io.sqlmask.introspect.TableKind;

import java.util.List;

/**
 * One table of an instance: ordered columns whose types are the engine's own
 * type name text (the YAML type per the dialect), never a parsed form.
 * {@code kind} is the normalized {@link TableKind} value collected from the
 * source engine ("table" for plain tables).
 */
public record TableStructure(String catalog, String schema, String name, String kind,
    List<ColumnStructure> columns) {

  public TableStructure {
    columns = List.copyOf(columns);
    kind = kind == null || kind.isBlank() ? TableKind.TABLE : kind;
  }

  /** Convenience constructor for plain tables (kind defaults to "table"). */
  public TableStructure(String catalog, String schema, String name,
      List<ColumnStructure> columns) {
    this(catalog, schema, name, TableKind.TABLE, columns);
  }

  public String qualifiedName() {
    return catalog + "." + schema + "." + name;
  }

  public record ColumnStructure(String name, String type) {
  }
}
```

`MetadataYamlImporter.parseTable` 两处改动——`return` 前解析 kind:

```java
    String catalog = requiredString(tableMap, "catalog", path);
    String schema = requiredString(tableMap, "schema", path);
    String name = requiredString(tableMap, "name", path);
    String kind = optionalKind(tableMap, path);
```

方法末尾:

```java
    return new TableStructure(catalog, schema, name, kind, columns);
```

新增私有方法(放在 `parseTable` 之后):

```java
  /** Optional normalized kind; an illegal value is a config error, never silently dropped. */
  private static String optionalKind(Map<?, ?> tableMap, String path) {
    Object node = tableMap.get("kind");
    if (node == null) {
      return TableKind.TABLE;
    }
    if (!(node instanceof String s) || !TableKind.isKnown(s)) {
      throw new SqlMaskException(SqlMaskException.Code.CONFIG_ERROR,
          path + ".kind: unknown kind '" + node + "' "
              + "(expected table|view|materialized_view)");
    }
    return s;
  }
```

import 区补 `io.sqlmask.introspect.TableKind`。

- [ ] **Step 4: 跑测试确认通过 + 服务层回归**

Run: `mvn -pl mask-metadata -am test -Dtest='MetadataYamlImporterTest,StructureServiceTest,CollectServiceTest,MetadataAdminControllerTest' -DfailIfNoTests=false`
Expected: PASS(四参构造器保证既有代码编译不变)。

- [ ] **Step 5: 提交**

```bash
git add mask-metadata/src/main/java/io/sqlmask/metaserver/model/TableStructure.java \
  mask-metadata/src/main/java/io/sqlmask/metaserver/service/MetadataYamlImporter.java \
  mask-metadata/src/test/java/io/sqlmask/metaserver/service/MetadataYamlImporterTest.java
git commit -m "feat(metaserver): TableStructure.kind 与 importer 枚举校验"
```

---

### Task 7: `CollectService` 透传 kind

**Files:**
- Modify: `mask-metadata/src/main/java/io/sqlmask/metaserver/service/CollectService.java:61`
- Test: `mask-metadata/src/test/java/io/sqlmask/metaserver/service/CollectServiceTest.java`

**Interfaces:**
- Consumes: Task 1 `TableInfo.kind()`、Task 6 五参 `TableStructure`。
- Produces: collect 存入的 `TableStructure.kind()` 等于 introspector 产出的 kind。

- [ ] **Step 1: 写失败测试**

`CollectServiceTest` 新增:

```java
  @Test
  void tableKindPassesThroughToStoredStructure() {
    IntrospectionResult viewResult = new IntrospectionResult("db", List.of(
        new IntrospectionResult.TableInfo("db", "public", "customer_v", "view",
            List.of(new IntrospectionResult.ColumnInfo("id", "bigint", "int8", false)))),
        List.of());
    CollectService viewService = new CollectService(instances, structures, ref -> "pw",
        engine -> spec -> viewResult);
    viewService.collect("pg_prod");
    assertEquals("view", store.loadStructure("pg_prod").get(0).kind());
  }
```

并在既有 `collectReplacesStructureAndReportsCounts` 断言区追加一行(该夹具走四参构造器,验证缺省值贯通):

```java
    assertEquals("table", stored.get(0).kind());
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-metadata -am test -Dtest=CollectServiceTest -DfailIfNoTests=false`
Expected: `tableKindPassesThroughToStoredStructure` FAIL(当前 `toStructures` 用四参构造,kind 恒为 `table`)。

- [ ] **Step 3: 最小实现**

`CollectService.toStructures` 的最后一行(原第 61 行):

```java
      tables.add(new TableStructure(table.catalog(), table.schema(), table.name(),
          table.kind(), columns));
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mvn -pl mask-metadata -am test -Dtest=CollectServiceTest -DfailIfNoTests=false`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add mask-metadata/src/main/java/io/sqlmask/metaserver/service/CollectService.java \
  mask-metadata/src/test/java/io/sqlmask/metaserver/service/CollectServiceTest.java
git commit -m "feat(metaserver): collect 透传 TableInfo.kind"
```

---

### Task 8: `meta_table.kind` 列、存量迁移与 store 读写

**Files:**
- Modify: `mask-metadata/src/main/resources/metadata-schema.sql:19-27`
- Modify: `mask-metadata/src/main/java/io/sqlmask/metaserver/store/JdbcMetaStore.java`(replaceStructure 第 131-134 行、loadStructure 第 150-159 行)
- Test: `mask-metadata/src/test/java/io/sqlmask/metaserver/store/JdbcMetaStoreTest.java`

**Interfaces:**
- Consumes: Task 6 五参 `TableStructure`(store 是它的唯一持久化方)。
- Produces: `meta_table.kind` 列(`VARCHAR(32) NOT NULL DEFAULT 'table'`);store 写读 kind 往返一致;存量库(无 kind 列)在启动时被 ALTER 补列,旧写入路径依旧可用。

- [ ] **Step 1: 写失败测试**

`JdbcMetaStoreTest` 改两处:

(a) 把 `ds` 提升为静态字段(`setUp` 里局部 `javax.sql.DataSource ds` 改为赋给 `private static javax.sql.DataSource ds;`),供迁移测试复用。

(b) 新增两个测试:

```java
  @Test
  void kindRoundTripsThroughStore() {
    String name = "it_" + UUID.randomUUID();
    store.createInstance(row(name));
    TableStructure plain = new TableStructure("crm", "public", "customer",
        List.of(new ColumnStructure("id", "bigint")));
    TableStructure view = new TableStructure("crm", "public", "customer_v", "view",
        List.of(new ColumnStructure("id", "bigint")));
    store.replaceStructure(name, List.of(plain, view));
    List<TableStructure> loaded = store.loadStructure(name);
    assertEquals("table", loaded.get(0).kind());
    assertEquals("view", loaded.get(1).kind());
  }

  @Test
  void schemaMigrationRestoresKindColumnWithDefault() throws Exception {
    // 模拟存量库：删掉 kind 列后重跑全量 DDL，ALTER ... ADD COLUMN IF NOT EXISTS 负责补列
    jdbc.execute("ALTER TABLE meta_table DROP COLUMN kind");
    try (Connection connection = ds.getConnection()) {
      ScriptUtils.executeSqlScript(connection,
          new ClassPathResource("/metadata-schema.sql", JdbcMetaStoreTest.class));
    }
    Integer kindColumns = jdbc.queryForObject(
        "SELECT COUNT(*) FROM information_schema.columns "
            + "WHERE table_name = 'meta_table' AND column_name = 'kind'", Integer.class);
    assertEquals(1, kindColumns);

    // 存量写入路径（INSERT 不带 kind 列）依旧可用，默认值补齐
    String name = "it_" + UUID.randomUUID();
    store.createInstance(row(name));
    jdbc.update("INSERT INTO meta_table (instance_id, catalog, schema_name, table_name, position) "
        + "SELECT id, 'crm', 'public', 'legacy_t', 0 FROM meta_instance WHERE name = ?", name);
    assertEquals("table", jdbc.queryForObject(
        "SELECT kind FROM meta_table WHERE table_name = 'legacy_t'", String.class));
  }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `mvn -pl mask-metadata -am test -Dtest=JdbcMetaStoreTest -DfailIfNoTests=false`
Expected: `kindRoundTripsThroughStore` FAIL(view 写入后读回缺省 `table`);`schemaMigrationRestoresKindColumnWithDefault` FAIL(DROP 后重跑脚本不补列,查询 kind 列报错/计数 0)。

- [ ] **Step 3: 最小实现**

(a) `metadata-schema.sql` 的 `meta_table` 建表加列(放 `table_name` 与 `position` 之间),文件末尾追加 ALTER:

```sql
CREATE TABLE IF NOT EXISTS meta_table (
  id BIGSERIAL PRIMARY KEY,
  instance_id BIGINT NOT NULL REFERENCES meta_instance(id) ON DELETE CASCADE,
  catalog VARCHAR(255) NOT NULL,
  schema_name VARCHAR(255) NOT NULL,
  table_name VARCHAR(255) NOT NULL,
  kind VARCHAR(32) NOT NULL DEFAULT 'table',
  position INT NOT NULL,
  UNIQUE (instance_id, catalog, schema_name, table_name)
);
```

```sql
-- 存量库补列：spring.sql.init mode=always 每次启动执行；新库上无效果（幂等）
ALTER TABLE meta_table ADD COLUMN IF NOT EXISTS kind VARCHAR(32) NOT NULL DEFAULT 'table';
```

(b) `JdbcMetaStore.replaceStructure` 的 INSERT(原第 131-134 行):

```java
        Long tableId = jdbc.queryForObject(
            "INSERT INTO meta_table (instance_id, catalog, schema_name, table_name, kind, position) "
                + "VALUES (?, ?, ?, ?, ?, ?) RETURNING id",
            Long.class, id, table.catalog(), table.schema(), table.name(), table.kind(),
            tablePosition++);
```

(c) `JdbcMetaStore.loadStructure`(原第 150-158 行)SELECT 加 kind 列并传入构造器:

```java
    List<TableStructure> tables = jdbc.query(
        "SELECT id, catalog, schema_name, table_name, kind FROM meta_table "
            + "WHERE instance_id = ? ORDER BY position", (rs, i) -> new TableStructure(
            rs.getString("catalog"), rs.getString("schema_name"), rs.getString("table_name"),
            rs.getString("kind"),
            jdbc.query(
                "SELECT name, type_declaration FROM meta_column WHERE table_id = ? ORDER BY position",
                (crs, ci) -> new ColumnStructure(crs.getString("name"),
                    crs.getString("type_declaration")),
                rs.getLong("id"))),
        id);
```

- [ ] **Step 4: 跑测试确认通过**

Run: `mvn -pl mask-metadata -am test -Dtest=JdbcMetaStoreTest -DfailIfNoTests=false`
Expected: PASS(既有 `crudLifecycleBumpsVersion` 用四参构造 + DB 默认 `'table'`,equals 依旧成立)。

- [ ] **Step 5: 提交**

```bash
git add mask-metadata/src/main/resources/metadata-schema.sql \
  mask-metadata/src/main/java/io/sqlmask/metaserver/store/JdbcMetaStore.java \
  mask-metadata/src/test/java/io/sqlmask/metaserver/store/JdbcMetaStoreTest.java
git commit -m "feat(metaserver): meta_table.kind 列、存量迁移与 store 读写"
```

---

### Task 9: 改写端容忍契约测试(mask-core,test-only)

**Files:**
- Test: `mask-core/src/test/java/io/sqlmask/config/YamlConfigLoaderTest.java`

**Interfaces:**
- Consumes: 既有 `YamlConfigLoader.loadContent`、`LoadedConfig.findTable(catalog, schema, name)`。
- Produces: 无生产接口——锁定「`kind:` 是合法的未知键,加载容忍且行为不变」的契约,防止未来有人把 loader 改成严格映射后,带 kind 的生成 YAML 反序列化失败。

- [ ] **Step 1: 写契约测试（现状预期直接通过,本任务是锁契约而非修 bug）**

先确认现状:该测试**预期直接 PASS**(loader 本就忽略未知键)。本任务的意义是锁契约,不是修 bug。新增:

```java
  @Test
  void kindFieldIsToleratedAndIgnored() {
    String yaml = """
        metadata:
          tables:
            - catalog: crm
              schema: public
              name: customer_v
              kind: view
              columns:
                - name: id
                  type: bigint
        policies: {}
        """;
    LoadedConfig loaded = loader.loadContent(yaml, "test.yaml");
    var table = loaded.findTable("crm", "public", "customer_v");
    assertTrue(table.isPresent(), () -> "kind 键不得影响表加载");
    assertEquals(List.of("id"), table.get().columns().stream().map(c -> c.name()).toList());
  }
```

若运行后失败,说明 loader 现实与预期不符——停下报告,不要顺手改 loader(改写端零改动是 spec 非目标)。

- [ ] **Step 2: 跑测试确认通过**

Run: `mvn -pl mask-core -am test -Dtest=YamlConfigLoaderTest -DfailIfNoTests=false`
Expected: PASS。

- [ ] **Step 3: 提交**

```bash
git add mask-core/src/test/java/io/sqlmask/config/YamlConfigLoaderTest.java
git commit -m "test(config): 锁定 YamlConfigLoader 对 kind 字段的容忍契约"
```

---

### Task 10: 全量回归

**Files:** 无新改动(纯验证)。

- [ ] **Step 1: 全模块测试**

Run: `mvn test`
Expected: BUILD SUCCESS,所有模块全绿(含 `GoldenOutputTest`/`MultiDialectGoldenOutputTest`——它们的 YAML 夹具没有 kind 键,预期零改动)。

- [ ] **Step 2: 确认工作区干净**

Run: `git status`
Expected: 无未提交改动(除执行前已存在的无关文件)。若 mask-lite 出现编译失败,说明它对 mask-core 有未声明的编译依赖——停下报告,不要改 mask-lite 生产代码。
