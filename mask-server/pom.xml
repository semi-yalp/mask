<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.sqlmask</groupId>
    <artifactId>sql-mask-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>mask-server</artifactId>
  <packaging>jar</packaging>

  <name>mask-server</name>
  <description>The modular-monolith application: composes every domain library
    (metadata, policy admin, query gateway, risk) over the pure mask-engine
    kernel. Same jar serves web mode and one-shot CLI mode.</description>

  <dependencies>
    <!-- kernel -->
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-engine</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-policy</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- shared service surface -->
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-common</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- domain libraries -->
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-metadata</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-policy-admin</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-query</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-risk</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- cross-cutting libraries -->
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-audit</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.sqlmask</groupId>
      <artifactId>mask-auth</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>info.picocli</groupId>
      <artifactId>picocli</artifactId>
      <version>${picocli.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- metadata/policy stores already bring the pg driver via mask-engine;
         explicit here so the server module fails fast if that ever changes -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.zonky.test</groupId>
      <artifactId>embedded-postgres</artifactId>
      <version>2.0.7</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>sqlmask-server</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.version}</version>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <executions>
          <execution>
            <goals><goal>repackage</goal></goals>
            <configuration>
              <mainClass>io.sqlmask.server.SqlMaskApplication</mainClass>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
