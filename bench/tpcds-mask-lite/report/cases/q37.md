# q37 — TPC-DS q37

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 6546 | 6.950 | same |
| 仅行过滤 | ✓ | 0 | — | 9302 | 16.583 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 7877 | 19.642 | same |

## 原始 SQL

```sql
SELECT i_item_id,
       i_item_desc,
       i_current_price
FROM item,
     inventory,
     date_dim,
     catalog_sales
WHERE i_current_price BETWEEN 68 AND 68 + 30
  AND inv_item_sk = i_item_sk
  AND d_date_sk=inv_date_sk
  AND d_date BETWEEN cast('2000-02-01' AS date) AND cast('2000-04-01' AS date)
  AND i_manufact_id IN (677,
                        940,
                        694,
                        808)
  AND inv_quantity_on_hand BETWEEN 100 AND 500
  AND cs_item_sk = i_item_sk
GROUP BY i_item_id,
         i_item_desc,
         i_current_price
ORDER BY i_item_id
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT i_item_id, i_item_desc, i_current_price
FROM item,
inventory,
date_dim,
catalog_sales
WHERE i_current_price BETWEEN ASYMMETRIC 68 AND 68 + 30 AND inv_item_sk = i_item_sk AND d_date_sk = inv_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-01' AS DATE) AND CAST('2000-04-01' AS DATE) AND i_manufact_id IN (677, 940, 694, 808) AND inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND cs_item_sk = i_item_sk
GROUP BY i_item_id, i_item_desc, i_current_price
ORDER BY i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT i_item_id, i_item_desc, i_current_price
FROM item,
inventory,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim,
catalog_sales
WHERE i_current_price BETWEEN ASYMMETRIC 68 AND 68 + 30 AND inv_item_sk = i_item_sk AND d_date_sk = inv_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-01' AS DATE) AND CAST('2000-04-01' AS DATE) AND i_manufact_id IN (677, 940, 694, 808) AND inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND cs_item_sk = i_item_sk
GROUP BY i_item_id, i_item_desc, i_current_price
ORDER BY i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT i_item_id, i_item_desc, i_current_price
FROM item,
inventory,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim,
catalog_sales
WHERE i_current_price BETWEEN ASYMMETRIC 68 AND 68 + 30 AND inv_item_sk = i_item_sk AND d_date_sk = inv_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-01' AS DATE) AND CAST('2000-04-01' AS DATE) AND i_manufact_id IN (677, 940, 694, 808) AND inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND cs_item_sk = i_item_sk
GROUP BY i_item_id, i_item_desc, i_current_price
ORDER BY i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
