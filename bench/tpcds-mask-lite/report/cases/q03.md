# q03 — TPC-DS q03

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 2358 | 3.401 | same |
| 仅行过滤 | ✓ | 0 | — | 2922 | 9.828 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 2774 | 10.287 | same |

## 原始 SQL

```sql
SELECT dt.d_year,
       i.i_brand_id brand_id,
       i.i_brand brand,
       sum(ss_ext_sales_price) sum_agg
FROM date_dim dt,
     store_sales ss,
     item i
WHERE dt.d_date_sk = ss.ss_sold_date_sk
  AND ss.ss_item_sk = i.i_item_sk
  AND i.i_manufact_id = 128
  AND dt.d_moy=11
GROUP BY dt.d_year,
         i.i_brand,
         i.i_brand_id
ORDER BY dt.d_year,
         sum_agg DESC,
         brand_id
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT dt.d_year, i.i_brand_id AS brand_id, i.i_brand AS brand, SUM(ss_ext_sales_price) AS sum_agg
FROM date_dim AS dt,
store_sales AS ss,
item AS i
WHERE dt.d_date_sk = ss.ss_sold_date_sk AND ss.ss_item_sk = i.i_item_sk AND i.i_manufact_id = 128 AND dt.d_moy = 11
GROUP BY dt.d_year, i.i_brand, i.i_brand_id
ORDER BY dt.d_year, sum_agg DESC, brand_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT dt.d_year, i.i_brand_id AS brand_id, i.i_brand AS brand, SUM(ss_ext_sales_price) AS sum_agg
FROM (SELECT *
FROM date_dim
WHERE d_year <= 2002) AS dt,
store_sales AS ss,
item AS i
WHERE dt.d_date_sk = ss.ss_sold_date_sk AND ss.ss_item_sk = i.i_item_sk AND i.i_manufact_id = 128 AND dt.d_moy = 11
GROUP BY dt.d_year, i.i_brand, i.i_brand_id
ORDER BY dt.d_year, sum_agg DESC, brand_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT dt.d_year, i.i_brand_id AS brand_id, i.i_brand AS brand, SUM(ss_ext_sales_price) AS sum_agg
FROM (SELECT *
FROM date_dim
WHERE d_year <= 2002) AS dt,
store_sales AS ss,
item AS i
WHERE dt.d_date_sk = ss.ss_sold_date_sk AND ss.ss_item_sk = i.i_item_sk AND i.i_manufact_id = 128 AND dt.d_moy = 11
GROUP BY dt.d_year, i.i_brand, i.i_brand_id
ORDER BY dt.d_year, sum_agg DESC, brand_id
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
