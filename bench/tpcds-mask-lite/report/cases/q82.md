# q82 — TPC-DS q82

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 3051 | 6.101 | same |
| 仅行过滤 | ✓ | 0 | — | 3138 | 17.440 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 3314 | 17.133 | same |

## 原始 SQL

```sql
SELECT it.i_item_id ,
       it.i_item_desc ,
       it.i_current_price
FROM item it,
     inventory inv,
     date_dim d1,
     store_sales ss
WHERE it.i_current_price BETWEEN 62 AND 62+30
  AND inv.inv_item_sk = it.i_item_sk
  AND d1.d_date_sk=inv.inv_date_sk
  AND d1.d_date BETWEEN cast('2000-05-25' AS date) AND cast('2000-07-24' AS date)
  AND it.i_manufact_id IN (129,
                        270,
                        821,
                        423)
  AND inv.inv_quantity_on_hand BETWEEN 100 AND 500
  AND ss.ss_item_sk = it.i_item_sk
GROUP BY it.i_item_id,
         it.i_item_desc,
         it.i_current_price
ORDER BY it.i_item_id
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT it.i_item_id, it.i_item_desc, it.i_current_price
FROM item AS it,
inventory AS inv,
date_dim AS d1,
store_sales AS ss
WHERE it.i_current_price BETWEEN ASYMMETRIC 62 AND 62 + 30 AND inv.inv_item_sk = it.i_item_sk AND d1.d_date_sk = inv.inv_date_sk AND d1.d_date BETWEEN ASYMMETRIC CAST('2000-05-25' AS DATE) AND CAST('2000-07-24' AS DATE) AND it.i_manufact_id IN (129, 270, 821, 423) AND inv.inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND ss.ss_item_sk = it.i_item_sk
GROUP BY it.i_item_id, it.i_item_desc, it.i_current_price
ORDER BY it.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT it.i_item_id, it.i_item_desc, it.i_current_price
FROM item AS it,
inventory AS inv,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1,
store_sales AS ss
WHERE it.i_current_price BETWEEN ASYMMETRIC 62 AND 62 + 30 AND inv.inv_item_sk = it.i_item_sk AND d1.d_date_sk = inv.inv_date_sk AND d1.d_date BETWEEN ASYMMETRIC CAST('2000-05-25' AS DATE) AND CAST('2000-07-24' AS DATE) AND it.i_manufact_id IN (129, 270, 821, 423) AND inv.inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND ss.ss_item_sk = it.i_item_sk
GROUP BY it.i_item_id, it.i_item_desc, it.i_current_price
ORDER BY it.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT it.i_item_id, it.i_item_desc, it.i_current_price
FROM item AS it,
inventory AS inv,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1,
store_sales AS ss
WHERE it.i_current_price BETWEEN ASYMMETRIC 62 AND 62 + 30 AND inv.inv_item_sk = it.i_item_sk AND d1.d_date_sk = inv.inv_date_sk AND d1.d_date BETWEEN ASYMMETRIC CAST('2000-05-25' AS DATE) AND CAST('2000-07-24' AS DATE) AND it.i_manufact_id IN (129, 270, 821, 423) AND inv.inv_quantity_on_hand BETWEEN ASYMMETRIC 100 AND 500 AND ss.ss_item_sk = it.i_item_sk
GROUP BY it.i_item_id, it.i_item_desc, it.i_current_price
ORDER BY it.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
