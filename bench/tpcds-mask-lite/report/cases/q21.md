# q21 — TPC-DS q21

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 3595 | 21.696 | same |
| 仅行过滤 | ✓ | 0 | — | 3650 | 32.716 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 3546 | 30.738 | same |

## 原始 SQL

```sql
SELECT *
FROM
    (SELECT w_warehouse_name,
            i_item_id,
            sum(CASE
                    WHEN (cast(d_date AS date) < CAST ('2000-03-11' AS date)) THEN inv_quantity_on_hand
                    ELSE 0
                END) AS inv_before,
            sum(CASE
                    WHEN (cast(d_date AS date) >= CAST ('2000-03-11' AS date)) THEN inv_quantity_on_hand
                    ELSE 0
                END) AS inv_after
     FROM inventory,
          warehouse,
          item,
          date_dim
     WHERE i_current_price BETWEEN 0.99 AND 1.49
       AND i_item_sk = inv_item_sk
       AND inv_warehouse_sk = w_warehouse_sk
       AND inv_date_sk = d_date_sk
       AND d_date BETWEEN CAST ('2000-02-10' AS date) AND CAST ('2000-04-10' AS date)
     GROUP BY w_warehouse_name,
              i_item_id) x
WHERE (CASE
           WHEN inv_before > 0 THEN (inv_after*1.000) / inv_before
           ELSE NULL
    END) BETWEEN 2.000/3.000 AND 3.000/2.000
ORDER BY w_warehouse_name NULLS FIRST,
         i_item_id NULLS FIRST
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT *
FROM (SELECT w_warehouse_name, i_item_id, SUM(CASE WHEN CAST(d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_before, SUM(CASE WHEN CAST(d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_after
FROM inventory,
warehouse,
item,
date_dim
WHERE i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i_item_sk = inv_item_sk AND inv_warehouse_sk = w_warehouse_sk AND inv_date_sk = d_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w_warehouse_name, i_item_id) AS x
WHERE CASE WHEN inv_before > 0 THEN inv_after * 1.000 / inv_before ELSE NULL END BETWEEN ASYMMETRIC 2.000 / 3.000 AND 3.000 / 2.000
ORDER BY w_warehouse_name NULLS FIRST, i_item_id NULLS FIRST
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT *
FROM (SELECT w_warehouse_name, i_item_id, SUM(CASE WHEN CAST(d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_before, SUM(CASE WHEN CAST(d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_after
FROM inventory,
warehouse,
item,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i_item_sk = inv_item_sk AND inv_warehouse_sk = w_warehouse_sk AND inv_date_sk = d_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w_warehouse_name, i_item_id) AS x
WHERE CASE WHEN inv_before > 0 THEN inv_after * 1.000 / inv_before ELSE NULL END BETWEEN ASYMMETRIC 2.000 / 3.000 AND 3.000 / 2.000
ORDER BY w_warehouse_name NULLS FIRST, i_item_id NULLS FIRST
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT *
FROM (SELECT w_warehouse_name, i_item_id, SUM(CASE WHEN CAST(d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_before, SUM(CASE WHEN CAST(d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN inv_quantity_on_hand ELSE 0 END) AS inv_after
FROM inventory,
warehouse,
item,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i_item_sk = inv_item_sk AND inv_warehouse_sk = w_warehouse_sk AND inv_date_sk = d_date_sk AND d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w_warehouse_name, i_item_id) AS x
WHERE CASE WHEN inv_before > 0 THEN inv_after * 1.000 / inv_before ELSE NULL END BETWEEN ASYMMETRIC 2.000 / 3.000 AND 3.000 / 2.000
ORDER BY w_warehouse_name NULLS FIRST, i_item_id NULLS FIRST
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
