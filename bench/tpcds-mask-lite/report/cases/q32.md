# q32 — TPC-DS q32

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 3030 | 4.587 | same |
| 仅行过滤 | ✓ | 0 | — | 2679 | 11.158 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 2477 | 11.700 | same |

## 原始 SQL

```sql
SELECT sum(cs_ext_discount_amt) AS "excess discount amount"
FROM catalog_sales ,
     item ,
     date_dim
WHERE i_manufact_id = 977
  AND i_item_sk = cs_item_sk
  AND d_date BETWEEN '2000-01-27' AND cast('2000-04-26' AS date)
  AND d_date_sk = cs_sold_date_sk
  AND cs_ext_discount_amt >
      ( SELECT 1.3 * avg(cs_ext_discount_amt)
        FROM catalog_sales ,
             date_dim
        WHERE cs_item_sk = i_item_sk
          AND d_date BETWEEN '2000-01-27' AND cast('2000-04-26' AS date)
          AND d_date_sk = cs_sold_date_sk )
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT SUM(cs_ext_discount_amt) AS "excess discount amount"
FROM catalog_sales,
item,
date_dim
WHERE i_manufact_id = 977 AND i_item_sk = cs_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk AND cs_ext_discount_amt > (SELECT 1.3 * AVG(cs_ext_discount_amt)
FROM catalog_sales,
date_dim
WHERE cs_item_sk = i_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk)
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT SUM(cs_ext_discount_amt) AS "excess discount amount"
FROM catalog_sales,
item,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE i_manufact_id = 977 AND i_item_sk = cs_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk AND cs_ext_discount_amt > (SELECT 1.3 * AVG(cs_ext_discount_amt)
FROM catalog_sales,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE cs_item_sk = i_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk)
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT SUM(cs_ext_discount_amt) AS "excess discount amount"
FROM catalog_sales,
item,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE i_manufact_id = 977 AND i_item_sk = cs_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk AND cs_ext_discount_amt > (SELECT 1.3 * AVG(cs_ext_discount_amt)
FROM catalog_sales,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS date_dim
WHERE cs_item_sk = i_item_sk AND d_date BETWEEN ASYMMETRIC '2000-01-27' AND CAST('2000-04-26' AS DATE) AND d_date_sk = cs_sold_date_sk)
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
