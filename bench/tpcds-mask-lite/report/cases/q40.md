# q40 — TPC-DS q40

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 3046 | 13.736 | same |
| 仅行过滤 | ✓ | 0 | — | 3452 | 18.344 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 3278 | 18.923 | same |

## 原始 SQL

```sql
SELECT w.w_state,
       i.i_item_id,
       sum(CASE
               WHEN (cast(dt.d_date AS date) < CAST ('2000-03-11' AS date)) THEN cs.cs_sales_price - coalesce(cr.cr_refunded_cash,0)
               ELSE 0
           END) AS sales_before,
       sum(CASE
               WHEN (cast(dt.d_date AS date) >= CAST ('2000-03-11' AS date)) THEN cs.cs_sales_price - coalesce(cr.cr_refunded_cash,0)
               ELSE 0
           END) AS sales_after
FROM catalog_sales cs
         LEFT OUTER JOIN catalog_returns cr ON (cs.cs_order_number = cr.cr_order_number
    AND cs.cs_item_sk = cr.cr_item_sk) ,
     warehouse w,
     item i,
     date_dim dt
WHERE i.i_current_price BETWEEN 0.99 AND 1.49
  AND I.i_item_sk = cs.cs_item_sk
  AND cs.cs_warehouse_sk = w.w_warehouse_sk
  AND cs.cs_sold_date_sk = dt.d_date_sk
  AND dt.d_date BETWEEN CAST ('2000-02-10' AS date) AND CAST ('2000-04-10' AS date)
GROUP BY w.w_state,
         i.i_item_id
ORDER BY w.w_state,
         i.i_item_id
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT w.w_state, i.i_item_id, SUM(CASE WHEN CAST(dt.d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_before, SUM(CASE WHEN CAST(dt.d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_after
FROM catalog_sales AS cs
LEFT JOIN catalog_returns AS cr ON cs.cs_order_number = cr.cr_order_number AND cs.cs_item_sk = cr.cr_item_sk,
warehouse AS w,
item AS i,
date_dim AS dt
WHERE i.i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i.i_item_sk = cs.cs_item_sk AND cs.cs_warehouse_sk = w.w_warehouse_sk AND cs.cs_sold_date_sk = dt.d_date_sk AND dt.d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w.w_state, i.i_item_id
ORDER BY w.w_state, i.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT w.w_state, i.i_item_id, SUM(CASE WHEN CAST(dt.d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_before, SUM(CASE WHEN CAST(dt.d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_after
FROM catalog_sales AS cs
LEFT JOIN catalog_returns AS cr ON cs.cs_order_number = cr.cr_order_number AND cs.cs_item_sk = cr.cr_item_sk,
warehouse AS w,
item AS i,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS dt
WHERE i.i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i.i_item_sk = cs.cs_item_sk AND cs.cs_warehouse_sk = w.w_warehouse_sk AND cs.cs_sold_date_sk = dt.d_date_sk AND dt.d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w.w_state, i.i_item_id
ORDER BY w.w_state, i.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT w.w_state, i.i_item_id, SUM(CASE WHEN CAST(dt.d_date AS DATE) < CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_before, SUM(CASE WHEN CAST(dt.d_date AS DATE) >= CAST('2000-03-11' AS DATE) THEN cs.cs_sales_price - COALESCE(cr.cr_refunded_cash, 0) ELSE 0 END) AS sales_after
FROM catalog_sales AS cs
LEFT JOIN catalog_returns AS cr ON cs.cs_order_number = cr.cr_order_number AND cs.cs_item_sk = cr.cr_item_sk,
warehouse AS w,
item AS i,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS dt
WHERE i.i_current_price BETWEEN ASYMMETRIC 0.99 AND 1.49 AND i.i_item_sk = cs.cs_item_sk AND cs.cs_warehouse_sk = w.w_warehouse_sk AND cs.cs_sold_date_sk = dt.d_date_sk AND dt.d_date BETWEEN ASYMMETRIC CAST('2000-02-10' AS DATE) AND CAST('2000-04-10' AS DATE)
GROUP BY w.w_state, i.i_item_id
ORDER BY w.w_state, i.i_item_id
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
