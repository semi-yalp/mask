# q70 — TPC-DS q70

结果概览（引擎 lite）：

| 模式 | 改写 | 退出码 | 错误码 | 冷耗时(ms) | 热身p50(ms) | 语义对拍 |
|---|---|---|---|---|---|---|
| 仅脱敏 | ✓ | 0 | — | 4037 | 16.466 | same |
| 仅行过滤 | ✓ | 0 | — | 4800 | 30.196 | same |
| 脱敏+行过滤 | ✓ | 0 | — | 3334 | 27.468 | same |

## 原始 SQL

```sql
SELECT sum(ss.ss_net_profit) AS total_sum,
       s.s_state,
       s.s_county,
       grouping(s.s_state)+grouping(s.s_county) AS lochierarchy,
       rank() OVER (PARTITION BY grouping(s.s_state)+grouping(s.s_county),
                                 CASE
                                     WHEN grouping(s.s_county) = 0 THEN s.s_state
                                 END
                    ORDER BY sum(ss.ss_net_profit) DESC) AS rank_within_parent
FROM store_sales ss,
     date_dim d1,
     store s
WHERE d1.d_month_seq BETWEEN 1200 AND 1200+11
  AND d1.d_date_sk = ss.ss_sold_date_sk
  AND s.s_store_sk = ss.ss_store_sk
  AND s.s_state IN
      (SELECT s.s_state
       FROM
           (SELECT s.s_state AS s_state,
                   rank() OVER (PARTITION BY s.s_state
                            ORDER BY sum(ss.ss_net_profit) DESC) AS ranking
            FROM store_sales ss,
                 store s,
                 date_dim d1
            WHERE d1.d_month_seq BETWEEN 1200 AND 1200+11
              AND d1.d_date_sk = ss.ss_sold_date_sk
              AND s.s_store_sk = ss.ss_store_sk
            GROUP BY s.s_state) tmp1
       WHERE ranking <= 5 )
GROUP BY rollup(s.s_state,s.s_county)
ORDER BY lochierarchy DESC ,
         CASE
             WHEN lochierarchy = 0 THEN s.s_state
             END ,
         rank_within_parent
    LIMIT 100
```

## 改写产物 — 仅脱敏

未命中任何策略（解析后规范化快照，无包装/注入）：

```sql
SELECT SUM(ss.ss_net_profit) AS total_sum, s.s_state, s.s_county, GROUPING(s.s_state) + GROUPING(s.s_county) AS lochierarchy, RANK() OVER (PARTITION BY GROUPING(s.s_state) + GROUPING(s.s_county), CASE WHEN GROUPING(s.s_county) = 0 THEN s.s_state ELSE NULL END ORDER BY SUM(ss.ss_net_profit) DESC) AS rank_within_parent
FROM store_sales AS ss,
date_dim AS d1,
store AS s
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk AND s.s_state IN (SELECT s.s_state
FROM (SELECT s.s_state AS s_state, RANK() OVER (PARTITION BY s.s_state ORDER BY SUM(ss.ss_net_profit) DESC) AS ranking
FROM store_sales AS ss,
store AS s,
date_dim AS d1
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk
GROUP BY s.s_state) AS tmp1
WHERE ranking <= 5)
GROUP BY ROLLUP(s.s_state, s.s_county)
ORDER BY lochierarchy DESC, CASE WHEN lochierarchy = 0 THEN s.s_state ELSE NULL END, rank_within_parent
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 仅行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT SUM(ss.ss_net_profit) AS total_sum, s.s_state, s.s_county, GROUPING(s.s_state) + GROUPING(s.s_county) AS lochierarchy, RANK() OVER (PARTITION BY GROUPING(s.s_state) + GROUPING(s.s_county), CASE WHEN GROUPING(s.s_county) = 0 THEN s.s_state ELSE NULL END ORDER BY SUM(ss.ss_net_profit) DESC) AS rank_within_parent
FROM store_sales AS ss,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1,
store AS s
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk AND s.s_state IN (SELECT s.s_state
FROM (SELECT s.s_state AS s_state, RANK() OVER (PARTITION BY s.s_state ORDER BY SUM(ss.ss_net_profit) DESC) AS ranking
FROM store_sales AS ss,
store AS s,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk
GROUP BY s.s_state) AS tmp1
WHERE ranking <= 5)
GROUP BY ROLLUP(s.s_state, s.s_county)
ORDER BY lochierarchy DESC, CASE WHEN lochierarchy = 0 THEN s.s_state ELSE NULL END, rank_within_parent
FETCH NEXT 100 ROWS ONLY;
```

## 改写产物 — 脱敏+行过滤

未命中脱敏策略，注入了行过滤谓词：

```sql
SELECT SUM(ss.ss_net_profit) AS total_sum, s.s_state, s.s_county, GROUPING(s.s_state) + GROUPING(s.s_county) AS lochierarchy, RANK() OVER (PARTITION BY GROUPING(s.s_state) + GROUPING(s.s_county), CASE WHEN GROUPING(s.s_county) = 0 THEN s.s_state ELSE NULL END ORDER BY SUM(ss.ss_net_profit) DESC) AS rank_within_parent
FROM store_sales AS ss,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1,
store AS s
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk AND s.s_state IN (SELECT s.s_state
FROM (SELECT s.s_state AS s_state, RANK() OVER (PARTITION BY s.s_state ORDER BY SUM(ss.ss_net_profit) DESC) AS ranking
FROM store_sales AS ss,
store AS s,
(SELECT *
FROM date_dim
WHERE d_year <= 2002) AS d1
WHERE d1.d_month_seq BETWEEN ASYMMETRIC 1200 AND 1200 + 11 AND d1.d_date_sk = ss.ss_sold_date_sk AND s.s_store_sk = ss.ss_store_sk
GROUP BY s.s_state) AS tmp1
WHERE ranking <= 5)
GROUP BY ROLLUP(s.s_state, s.s_county)
ORDER BY lochierarchy DESC, CASE WHEN lochierarchy = 0 THEN s.s_state ELSE NULL END, rank_within_parent
FETCH NEXT 100 ROWS ONLY;
```

## 问题与解决思路

三种模式均成功改写，无策略/方言相关问题。共性说明见 REPORT.md「已知限制」：注释与原始排版不保真（内层为解析后快照）、`LIMIT` 规范化为 `FETCH NEXT … ROWS ONLY`、大小写折叠为小写等。
