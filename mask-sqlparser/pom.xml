<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.sqlmask</groupId>
    <artifactId>sql-mask-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>mask-sqlparser</artifactId>

  <dependencies>
    <dependency>
      <groupId>org.apache.calcite</groupId>
      <artifactId>calcite-core</artifactId>
      <version>${calcite.version}</version>
    </dependency>
    <!-- 语法基底（main scope）：本项目 codegen 以 calcite-babel 1.42.0 的
         codegen 数据（config.fmpp + includes）为基底生成解析器，产生的
         SqlMaskParserImpl 引用 org.apache.calcite.sql.babel.* 节点类，
         运行期必需；BabelEquivalenceTest 另用其 SqlBabelParserImpl 做差分
         基准（spec §7.2）。 -->
    <dependency>
      <groupId>org.apache.calcite</groupId>
      <artifactId>calcite-babel</artifactId>
      <version>${calcite.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>com.googlecode.fmpp-maven-plugin</groupId>
        <artifactId>fmpp-maven-plugin</artifactId>
        <version>1.0</version>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals><goal>generate</goal></goals>
          </execution>
        </executions>
        <configuration>
          <cfgFile>src/main/codegen/config.fmpp</cfgFile>
          <templateDirectory>src/main/codegen/templates</templateDirectory>
          <template>Parser.jj</template>
          <outputDirectory>target/generated-sources/fmpp</outputDirectory>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>javacc-maven-plugin</artifactId>
        <version>2.6</version>
        <dependencies>
          <!-- 上游 calcite 1.42 的 Gradle 构建固定用 javacc 4.0
               （buildSrc JavaCCPlugin.kt 硬编码）；插件 2.6 自带的 5.0 会把
               switch 的 case 动作块摊平进同一作用域，babel 产生式
               （InfixCast / SelectExpression 的 EXCLUDE/REPLACE 分支）里
               同名局部变量声明两次导致编译失败。 -->
          <dependency>
            <groupId>net.java.dev.javacc</groupId>
            <artifactId>javacc</artifactId>
            <version>4.0</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <goals><goal>javacc</goal></goals>
          </execution>
        </executions>
        <configuration>
          <sourceDirectory>target/generated-sources/fmpp</sourceDirectory>
          <includes><include>**/Parser.jj</include></includes>
          <outputDirectory>target/generated-sources/javacc</outputDirectory>
          <!-- 上游 JavaCCTask 以 -LOOKAHEAD:2 运行（babel/build.gradle.kts） -->
          <lookAhead>2</lookAhead>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
